import Card from '../../components/card/Card';
import './applyFreelancer.css';

const Skills = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>Skills</Card>
    )
}

export default Skills;