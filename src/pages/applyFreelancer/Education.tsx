import Card from '../../components/card/Card';
import './applyFreelancer.css';

const Education = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>Education</Card>
    )
}

export default Education;