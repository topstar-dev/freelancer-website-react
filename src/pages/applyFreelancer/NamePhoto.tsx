import Card from '../../components/card/Card';
import './applyFreelancer.css';

const NamePhoto = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>NamePhoto</Card>
    )
}

export default NamePhoto;