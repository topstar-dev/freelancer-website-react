import Card from '../../components/card/Card';
import './applyFreelancer.css';

const Experience = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>Experience</Card>
    )
}

export default Experience;