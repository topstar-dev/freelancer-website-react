import Card from '../../components/card/Card';
import './applyFreelancer.css';

const AboutMe = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>AboutMe</Card>
    )
}

export default AboutMe;