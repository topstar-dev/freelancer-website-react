import Card from '../../components/card/Card';
import './applyFreelancer.css';

const Languages = (props: any) => {
    return (
        <Card className={`rounx-freelancer-card`}>Languages</Card>
    )
}

export default Languages;